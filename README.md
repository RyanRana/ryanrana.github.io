# Ryan Rana's Portfolio

Personal website with JSON-based content management. Edit content in `/content` folder, run local server with `./start.sh` or `python3 -m http.server 8000`, view at http://localhost:8000.
